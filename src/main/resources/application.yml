spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/serviceLayerDB
    username: admin
    password: admin
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none